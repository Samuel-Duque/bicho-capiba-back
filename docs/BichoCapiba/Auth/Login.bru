meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "ze@gmail.com",
    "password": "secret"
  }
}

settings {
  encodeUrl: true
}
